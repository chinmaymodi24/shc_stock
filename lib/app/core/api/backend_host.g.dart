// GENERATED FILE — do not edit by hand.
//
// Rewritten at Gradle configuration time by android/app/build.gradle.kts with
// the dev machine's current LAN IPv4, so a physical phone on the same Wi-Fi
// reaches the local backend without USB or `adb reverse`. Committed empty and
// marked --skip-worktree so the per-machine IP never shows up in `git status`.
//
// Empty string  → fall back to `localhost` (emulator / USB / web / release).
const String kGeneratedDevBackendHost = '';
