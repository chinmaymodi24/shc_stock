<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Debug-only: allow plain HTTP so a physical phone can reach the dev
         backend over Wi-Fi at http://<LAN-IP>:4000. Android blocks cleartext
         by default from targetSdk 28 and only exempts `localhost`, so without
         this the app works over `adb reverse` (cable) and fails on Wi-Fi.
         Release builds don't include this source set and stay HTTPS-only. -->
    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        tools:targetApi="28" />
</manifest>
